---
# vars file for dns-server-install
bind_domain: "{{ domain_name }}"
bind_host: "{{ dns_host | default('dns-server')}}"
bind_ip: "{{ dns_ip  | default('10.1.77.5')}}"
bind_port: 53
bind_forwarder1: 8.8.8.8
bind_forwarder2: 8.8.4.4
bind_ansible_tag: "ANSIBLE MANAGED ZONE"